using UnityEngine;
using System.Collections;

public class HudControler : MonoBehaviour {

	public GameObject topSquare;
	public GameObject bottomSquare;
	public GameObject iconSquare;

	/*
	// Use this for initialization
	void Start () {
		int height = Screen.height;
		int width = Screen.width;

		int rectangleWidth = width * 0.3;

		RectTransform bottomTransform = bottomSquare.GetComponent<RectTransform> ();
		RectTransform topTransform = topSquare.GetComponent<RectTransform> ();
		RectTransform iconTransform = iconSquare.GetComponent<RectTransform> ();

		Rect desiredPosition;
		desiredPosition = new Rect (0f, 0f, rectangleWidth, height / 2);
		topTransform.rect = desiredPosition;

		desiredPosition = new Rect (0f, height / 2, rectangleWidth, height / 2);
		bottomTransform.rect = desiredPosition;
	
	}
	*/
	// Update is called once per frame
	void Update () {
	
	}
}
